#include<bits/stdc++.h>
using namespace std;

int helper(vector<int>&h,int i,int ct,int prev){
    if (h[i]>=prev) return 1;
    else{
        
    }
}

int main(){
    int n;
    cin>>n;
    vector<int>h(n);
    for (int i=0;i<n;i++) cin>>h[i];
    int temp;
    
}